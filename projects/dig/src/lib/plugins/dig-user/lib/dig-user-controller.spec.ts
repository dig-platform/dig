import { DigUserController } from './dig-user-controller';

describe('DigUserController', () => {
  it('should create an instance', () => {
    expect(new DigUserController()).toBeTruthy();
  });
});
